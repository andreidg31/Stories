import { makeStyles } from "@material-ui/core";

const postsStyles = makeStyles((theme) => ({
    container: {
        display: 'flex',
        flexWrap: 'wrap',
        flexDirection: 'row'
    }
}));

export default postsStyles;